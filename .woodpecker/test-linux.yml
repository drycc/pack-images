matrix:
  platform:
  - linux/amd64
  - linux/arm64

platform: ${platform}

labels:
  type: exec

pipeline:
- name: test-linux
  image: bash
  commands:
  - make test-style
  secrets:
  - codename
  - dev_registry
  - codecov_token
  - drycc_registry
  when:
    event:
    - push
    - tag
    - pull_request

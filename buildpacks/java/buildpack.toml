# Buildpack API version
api = "0.9"

# Buildpack ID and metadata
[buildpack]
id = "drycc/java"
version = "0.0.1"
name = "Java Buildpack"
homepage = "https://github.com/drycc/pack-images/tree/main/buildpacks/java"

# Stacks that the buildpack will work with
[[stacks]]
id = "drycc-bullseye"

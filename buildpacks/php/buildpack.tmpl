# Buildpack API version
api = "{BUILDPACK_API}"

# Buildpack ID and metadata
[buildpack]
id = "drycc/php"
version = "0.0.1"
name = "PHP Buildpack"
homepage = "https://github.com/drycc/pack-images/tree/main/buildpacks/php"

# Stacks that the buildpack will work with
[[stacks]]
id = "{STACK_ID}"
